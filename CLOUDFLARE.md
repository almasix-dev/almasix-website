# Cloudflare Pages (when connecting this repo)

Build command: `npm ci && npm run build`
Build output directory: `dist`
Node version: `22`

Root directory: `/` (repository root)

Custom domains (planned):
- `almasix.com`
- `www.almasix.com`
